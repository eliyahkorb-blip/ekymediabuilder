(() => {
  const root = document.getElementById('root');
  if (root) {
    root.innerHTML = '<div style="font-family:system-ui;padding:32px;color:#f4f4f2;background:#111;min-height:100vh"><h1>EKY Builder v7.1</h1><p>Bitte die App über <strong>START.bat</strong> starten. Dabei wird zuerst das lokale React-Bundle aus <code>src/app.jsx</code> gebaut.</p><p>Falls diese Meldung in der Desktop-App erscheint: im Terminal <code>npm install</code> und danach <code>npm start</code> ausführen.</p></div>';
  }
})();
